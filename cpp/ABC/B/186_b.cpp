#include <bits/stdc++.h>
#define rep(i,n) for(int i = 0; i < (n); ++i)
using namespace std;
using ll = long long;
using P = pair<int,int>;

ll mod = 1000000007;
int main() {
  int H,W = 0;
  cin >> H >> W;
  vector< vector<int>> vec(H, vector<int> (W,0));
  rep(i,H) {
    rep(j,W) {
      cin
    }
  }
  return 0;
}

