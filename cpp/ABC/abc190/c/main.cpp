#include <bits/stdc++.h>
using namespace std;
#include <atcoder/all>
using namespace atcoder;
#define rep(i,n) for(int i = 0; i < (n); ++i)
using ll = Long Long;
using P = pair<int,int>;

int main() {
  int n,m;
  vector<int> a(m), b(m);
  rep(i,m) cin >> a[i], b[i];
  int k;
  cin >> k;
  vector<int> c(k), d(k);
   
  return 0;
}
